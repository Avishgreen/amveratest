---
version: null
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10
build:
  requirementsPath: requirements.txt
run:
  command: streamlit run app.py --server.port=80
  persistenceMount: /data
  containerPort: 80
serviceType: compute
